
// Este archivo está preparado para la configuración de Firebase.
// Reemplaza los valores de marcador de posición con tus credenciales reales de Firebase.
// No es necesario modificar el resto de la aplicación para que funcione con Firebase,
// solo necesitas implementar la lógica de `api.ts` usando el SDK de Firebase.

/*
import { initializeApp } from "firebase/app";
import { getAuth } from "firebase/auth";
import { getFirestore } from "firebase/firestore";

const firebaseConfig = {
  apiKey: "AIzaSyCP16XTbwZ-v4clYgZYQ9TUwEdCIBNcMQ5A",
  authDomain: "parkisy.firebaseapp.com",
  projectId: "parkisy",
  storageBucket: "parkisy.firebasestorage.app",
  messagingSenderId: "265602472072",
  appId: "1:265602472072:web:e166690feb793e8a52a73d"
};

// Inicializar Firebase
const app = initializeApp(firebaseConfig);
export const auth = getAuth(app);
export const db = getFirestore(app);

*/

// Exportaciones simuladas para que el resto de la app compile
export const auth = {};
export const db = {};
